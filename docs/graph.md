---
hide:
  - toc
---

# Knowledge Graph

How the notes connect — teal lines are explicit links between pages, amber lines are pages that merely share a tag. Node size grows with how connected a page is. Drag a node to reposition it, click one to open that page.

<div id="kg-status" style="color: var(--md-default-fg-color--light); font-size: 0.85rem; margin-bottom: 0.5rem;">Loading…</div>
<canvas id="kg-canvas" data-base="." style="width: 100%; border: 1px solid var(--md-default-fg-color--lightest, rgba(0,0,0,0.1)); border-radius: 8px;"></canvas>

Regenerated by `scripts/generate_graph.py` on every sync. A page with no lines reaching it isn't cross-linked or tag-matched with anything yet — a real, if unglamorous, signal about which notes are still islands.
