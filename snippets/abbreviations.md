<!--
  Included via `--8<-- "abbreviations.md"` (pymdownx.snippets) at the bottom
  of every generated page. Defines pymdownx.abbr tooltips site-wide instead
  of per-page. Keep in sync with docs/glossary.md.
-->

*[CRD]: CustomResourceDefinition — teaches Kubernetes a new resource type
*[CR]: Custom Resource — an instance of a CRD
*[ISR]: In-Sync Replicas — Kafka broker replicas fully caught up with the leader
*[cp-schema]: Confluent Platform's schema-registry component
*[mTLS]: mutual TLS — both sides of a connection present certificates
